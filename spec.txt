# Programming Langauge Specification

A C-like compiler!

## Function Syntax

func function_name(type p1, type p2) -> return_type {
    return_type a;

    a = func(a, func2(p1, p2));

    int x = a + b * (2 + 3);

    return a;
}

int main() {
    // Program starts here

    return 0;
}
